# Requirements for Furniture Office Application Automated Testing
# Robot Framework and related libraries
robotframework >= 7.0.0
robotframework-seleniumlibrary >= 6.0.0

# Selenium WebDriver
selenium >= 4.0.0
webdriver-manager >= 3.8.0

# Additional libraries for testing
requests >= 2.28.0
beautifulsoup4 >= 4.11.0
lxml >= 4.9.0

# Optional: For parallel test execution
pabot >= 1.0.0

# Optional: For test reporting
robotframework-jsonlibrary >= 0.5
robotframework-requests >= 0.9.0